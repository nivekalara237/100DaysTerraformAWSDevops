// import * as cdk from 'aws-cdk-stacks';
// import { Template } from 'aws-cdk-stacks/assertions';
// import * as Day013 from '../stacks/day_014-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in stacks/main.stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new Day013.Day013Stack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
})
